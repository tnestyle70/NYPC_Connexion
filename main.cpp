#include <bits/stdc++.h>
using namespace std;

// Connexion submission-shaped placeholder.
// 실제 규칙, 입출력 protocol, action format이 확정되면 bot snapshot에서 구체화한다.

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string line;
    while (getline(cin, line)) {
        if (line == "FINISH" || line == "END") {
            break;
        }
    }

    return 0;
}

